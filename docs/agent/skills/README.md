# 技能清单

当前可用 skill（可复制到 `.codex/skills`）：

- `charm-init-graph`
  - 用途：新增能力（UART/SPI/I2C/Flash 等）时，规范化 board_caps、driver 适配、init.node 与 bringup 装配流程。
  - 触发场景：新增平台能力、接入 init.graph、标准化板级装配。

## 计划中的技能
- `charm-io-contracts`：Channel/Reactor/Registry 契约与落地检查清单
- `charm-docs-update`：文档索引与 README 同步流程
