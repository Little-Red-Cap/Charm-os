name: STM32 (arm-none-eabi 15.2.rel1)

on:
  push:
    branches: ["main", "dev"]
  pull_request:
    branches: ["main", "dev"]

jobs:
  stm32:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Arm GNU Toolchain 15.2.rel1
        uses: carlosperate/arm-none-eabi-gcc-action@v1
        with:
          release: "15.2.Rel1"

      # 固定使用新版 CMake + Ninja，避免 runner 自带 CMake 版本 < 4.0
      - name: Install CMake 4.1.2 and Ninja
        uses: lukka/get-cmake@latest
        with:
          cmakeVersion: 4.1.2
          ninjaVersion: latest

      - name: Verify toolchain
        run: |
          arm-none-eabi-gcc --version
          cmake --version
          ninja --version

      - name: Configure (STM32 Ink example)
        run: |
          cmake -G Ninja `
            -S Examples/ink/stm32f103c8 `
            -B build-stm32 `
            -DCMAKE_BUILD_TYPE=Release

      - name: Build (STM32 Ink)
        run: |
          cmake --build build-stm32

      - name: Size
        run: |
          arm-none-eabi-size build-stm32/*.elf || true
