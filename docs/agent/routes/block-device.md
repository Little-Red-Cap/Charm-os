# Block Device Route（OnlyCore 已退役）

> OnlyCore 不包含 block device、MAL、cache 或 VFS 实现。本页只保留旧主线的路由名称，
> 相关正文已移入 `docs/archive/`；不要据此新增当前 Core 入口。

## 文档状态

- `status`: `archived`
- `scope`: 提纯前存储路由的历史说明

当前 OnlyCore 的活动入口见 `docs/architecture/charm_core_contract.md` 和关系示例。
