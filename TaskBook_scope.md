# 便携式示波器（scope）任务书

## 项目目标
- 在 PC 端完成“便携式示波器”最小可用验证，驱动架构成熟。
- 目标可迁移到 MCU 运行，接口与依赖保持可裁剪性。

## 范围与约束
- 数据源：模拟波形（正弦/方波/噪声），单通道。
- 采样率：1 MHz（PC 侧模拟）。
- 触发：最小策略（电平 + 上升/下降沿）。
- 显示模式：滚动 / 触发锁定。
- 测量：峰峰值 Vpp、频率 Freq。
- 输入：触屏（鼠标模拟）、编码器（鼠标滚轮模拟）。
- 禁止动态内存、异常、RTTI（遵循项目硬性约束）。

## 里程碑
1. **M1：项目骨架与最小 UI**
   - scope 项目结构落地（已完成）
   - 模拟波形生成 + 触发 + 测量 + Vivid UI 渲染
2. **M2：波形专用控件**
   - 新增 `scope.waveform` 控件（固定 Y 轴范围、网格、触发标记）
   - 替换 Chart，支持滚动/触发锁定两种模式
3. **M3：交互与参数体系**
   - 时基/电压刻度调整
   - 触发电平拖拽
   - 触发边沿切换
4. **M4：可移植性准备**
   - 抽离平台输入适配层
   - 清理 PC 端依赖点，形成 MCU 接入模板

## 模块划分（建议）
- `scope.app`：采样、触发、测量、视图模型
- `scope.waveform`：波形渲染控件（Vivid）
- `scope.input`：输入映射（触屏/编码器抽象）
- `scope.platform.win`：SDL3 输入、窗口与渲染

## 接口要点（约束性）
- `scope::AppConfig`：采样率、帧长、波形参数、触发参数、显示模式
- `scope::App`：`tick()`、`set_trigger()`、`set_display_mode()`、`chart_points()`、`measurements()`
- `scope::Measurements`：`vpp`、`freq_hz`

## 验证方式
- 启动后 UI 显示波形，触发电平拖动可改变触发点
- 触发锁定模式下波形稳定
- 频率与 Vpp 在模拟波形变化时同步更新

## 风险与改进项
- 现用 `Chart` 会自动归一化，无法体现电压刻度；必须替换为波形控件。
- 现阶段仅单通道，后续扩展需考虑多通道布局与同步触发策略。
