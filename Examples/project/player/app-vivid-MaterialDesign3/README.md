# Player UI - Material Design 3

本目录为播放器的 **Material Design 3 风格 UI 变体**。目标是仅替换界面与视觉语言，不改动 `app-common` 的后端/文件系统/播放逻辑。

## 设计方向（第一阶段）
- 深色基底 + 低饱和主题色，强调“柔和对比”
- 更大的圆角与更厚的留白
- 按钮与列表统一“胶囊/圆角矩形”语言

## 说明
- 模块名仍为 `player.ui` / `player.ui_builder`，通过独立 target 编译避免与旧 UI 冲突
- 仅调整 UI 与主题，不影响控制面/数据面

