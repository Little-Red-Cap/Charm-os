# input-pump-win-demo

Minimal verification chain:

hal_input / RawInputSource -> input.pump / EDA -> app(out)

Cases expected:

1) normal input: at least one "[raw]" line
2) no input: no "[raw]" lines

Run:

```
./verify.ps1
```
