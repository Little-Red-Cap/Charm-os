# 最小内核运行时证据矩阵

这页不是新契约，也不是重写现有语义，而是把“哪条 seam 由哪个 verifier 证明”对齐到一张表里。

它把两类证据分开：

- 上半层 host / stub 证据：证明运行时语义、命名和最小闭环
- 下半层 ingress / ARMv7-A 映射证据：只在这里做引用，不在本页展开

QEMU 叶子和板级落地仍然是单独的证据线，不属于这页的编辑范围。

## 读法

- `what it proves` 只写这一层已经确认的事实
- `what it does not prove` 专门标出边界，避免把 host 证据误读成 leaf 证据
- `handoff` 说明这层证据继续向上或向下接哪条线

## 证据矩阵

| Layer / seam | Primary boundary | Primary verifier(s) | What it proves | What it does not prove | Handoff |
| --- | --- | --- | --- | --- | --- |
| `RuntimeBridge bind_idle / bind_trace` | `kernel.runtime_bridge` | `Examples/kernel/runtime_bridge_binding_host` | stateful idle/trace rebinding, getter 对齐, trace reroute, fallback idle retarget | 不证明 worker bootstrap、tick drain、ISR defer、thread port | 接 `runtime_run_loop` 与 `RuntimeThreadPort` |
| `RuntimeLoopPort` | `kernel.runtime_bridge` / lower-half loop port | `Examples/kernel/runtime_loop_port_host` | type-erased lower-half loop entry for `advance_tick`、`defer_from_isr`、`bootstrap_idle`、`bootstrap_worker`、`run_once_or_idle` | 不证明 thread-side `yield/sleep`、trap/syscall transport | 接 `runtime_run_loop`、`runtime_tick`、`runtime_isr_defer`、ARMv7-A leaf |
| `bootstrap_idle / bootstrap_worker / run_once_or_idle` | `kernel.runtime_glue` / `kernel.runtime_bridge` | `Examples/kernel/runtime_run_loop_host` | direct idle bootstrap, worker bootstrap, empty-run fallback to idle, invalid idle binding rejection | 不证明 tick drain、ISR defer、thread-side port、trap transport | 接 `runtime_tick`、`runtime_isr_defer`、`RuntimeThreadPort` |
| `runtime_advance_tick` | `kernel.runtime_glue` / `runtime_bridge` | `Examples/kernel/runtime_tick_host` | `advance_tick(now)` 的未到期/到期路径，timer drain，tick trace 对齐 | 不证明硬件 timer、IRQ 路由、trap ingress | 接 `runtime_bridge` 和 `scheduler/timer` |
| `runtime_defer_from_isr` | `kernel.runtime_glue` / `runtime_bridge` | `Examples/kernel/runtime_isr_defer_host` | ISR defer、scheduler post、worker resume 的最小闭环 | 不证明 VBAR/GIC/leaf exception frame | 接 `runtime_bridge` 和 `scheduler/post` |
| `RuntimeThreadPort` | `kernel.runtime_bridge` / `thread port` | `Examples/kernel/runtime_thread_port_host` | `yield_current`、`sleep_current_until`、idle/worker 端口语义 | 不证明 trap/syscall transport | 接 `thread / timer` |
| `RuntimeMailbox` | `kernel.runtime_mailbox` | `Examples/kernel/runtime_mailbox_host` | stateful `send / recv(timeout) / reply`，request/reply queue，timeout cancel，与 `server / client / idle / tick` 的最小 host 闭环 | 不证明 trap/syscall ingress、user ABI、multi-endpoint routing | 接 future task message / syscall surface |
| `TaskMessageApi` | `kernel.task_message_api` | `Examples/kernel/runtime_task_message_host`, `Examples/kernel/runtime_mailbox_host` | `RuntimeMailbox` 之上的 current-task message naming，默认未绑定占位、`bind_mailbox(...)`、task-named `send / receive / reply / wait_*` 入口，以及经由同一 API 驱动的 `server / client / idle / tick` host 闭环 | 不证明 trap/syscall ingress、user ABI、service discovery | 接 `task message table` 与 future IPC / syscall surface |
| `TaskMessageTable` | `kernel.task_message_table` | `Examples/kernel/runtime_task_message_table_host` | server-side `label -> handler` lookup、late bind、handled/unhandled 边界，以及将 mailbox request 收成稳定 table seam 的 host 证据 | 不证明 transport/wait loop/reply send、catalog/discovery、user ABI | 接 future service task routing / IPC protocol |
| `TaskMessageDispatch` | `kernel.task_message_dispatch` | `Examples/kernel/runtime_task_message_dispatch_host` | `TaskMessageApi + TaskMessageTable` 的 `receive -> dispatch -> reply` bridge，direct dispatch、单次 `serve_once()`、handled/replied 分离，以及 missing label / reply failure 的 host 证据 | 不证明 receive wait policy、timeout 装配、multi-request drain、service discovery、user ABI | 接 `task message service loop` |
| `TaskMessageServiceLoop` | `kernel.task_message_service_loop` | `Examples/kernel/runtime_task_message_loop_host` | server-side `wait_receive_until -> timeout consume/rearm -> dispatch/reply` live loop，基于真实 `RuntimeBridge + RuntimeMailbox + scheduler loop` 形成最小 service task 证据 | 不证明 budgeted multi-request drain、公平性、service discovery、higher-level message schema、user ABI | 接 `task message service drain` |
| `TaskMessageServiceDrain` | `kernel.task_message_service_drain` | `Examples/kernel/runtime_task_message_drain_host` | server-side 单次唤醒内的 budgeted `dispatch -> drain -> stop(boundary)` live loop；同一次 server wake 最多可消费 `N` 条排队 request，并显式收口 `budget_reached / queue_empty` 边界 | 不证明跨唤醒公平性策略、service discovery、higher-level message schema、user ABI | 接 future service task / IPC protocol |
| `TaskMessageServicePump` | `kernel.task_message_service_pump` | `Examples/kernel/runtime_task_message_pump_host` | server task body 级别的 `bootstrap wait -> timeout rearm -> drain hold/rearm` orchestration live loop；把 bootstrap、queue-empty rearm、budget hold 和 stale-ready rearm-fail 边界收成稳定上层 seam | 不证明更高层 protocol schema、service discovery、跨 service fairness、user ABI | 接 future protocol progress task / IPC protocol |
| `TaskMessage syscall bridge` | `kernel.task_message_syscall_bridge` | `Examples/kernel/runtime_task_message_syscall_host` | 第一条真实的 `message runtime -> syscall semantic` 上半层焊缝：`label -> TaskSyscallId`、`value -> arg0`、`TrapResult.value -> reply`，以及 unsupported message-side syscall 由 bridge trace 和 reply timeout 显式暴露 | 不证明完整多参数 message-borne syscall ABI、numbered frame、arch ingress、user ABI | 接 future message-frame syscall transport / `task_syscall_frame` |
| `TaskMessage syscall frame transport` | `kernel.task_message_syscall_frame` | `Examples/kernel/runtime_task_message_syscall_frame_host` | 显式 `message runtime -> syscall frame transport v1`：固定 request label + frame token、按 `(from, token)` 解析上半层 frame store、把 `capability_call` 多参数 frame 送入既有 `task_syscall_frame` 闭环；invalid syscall 写回完成后仍可 reply，而 missing token 保持 timeout | 不证明真实 arch frame 捕获/写回、user ABI、跨 task frame 共享策略、零拷贝 transport | 接 `task_syscall_frame` 与 future `trap ingress` |
| `TaskMessage syscall frame caller` | `kernel.task_message_syscall_frame_caller` | `Examples/kernel/runtime_task_message_syscall_frame_caller_host` | client-side `TaskSyscallRequest -> frame publish -> message send/wait -> reply completion` glue：顺序发起 `yield / sleep_until / capability_call / invalid syscall`，通过既有 message-frame transport 收回稳定 `TrapResult`，并保留 missing token timeout 证据 | 不证明同步阻塞的 remote syscall facade、多 outstanding 并发策略、跨地址空间 transport、user ABI | 接 future remote syscall facade / IPC RPC |
| `TaskMessage syscall client` | `kernel.task_message_syscall_client` | `Examples/kernel/runtime_task_message_syscall_client_host` | 在 caller 之上补一层 task-facing async client seam：用 `sys_*` 命名发起远端 syscall，用 `step(event)` 统一收口 reply / timeout；保持 one-pending-call，不把 timeout 伪装成 trap failure | 不证明同步阻塞的 remote syscall facade、多 outstanding 并发与公平性、跨地址空间 transport、user ABI | 接 future remote syscall facade / IPC RPC |
| `TaskMessage syscall pump` | `kernel.task_message_syscall_pump` | `Examples/kernel/runtime_task_message_syscall_pump_host` | 在 client 之上补一层 queued client orchestration seam：允许一次排队多条 `TaskSyscallRequest`，由 `kick()` 发出首条、由 `step(event)` 收口 reply / timeout completion，并在 client 空闲后自动续发下一条；保持下层 transport 始终只有一条 in-flight request，并显式暴露 `completion_drop` 边界 | 不证明真正多 outstanding 的 remote syscall transport、同步阻塞 RPC facade、跨 client 队列公平性、service discovery、arch ingress、user ABI | 接 `task_message_runtime_service` |
| `TaskMessage runtime service` | `kernel.task_message_runtime_service` | `Examples/kernel/runtime_task_message_runtime_service_host`, `Examples/kernel/runtime_task_message_syscall_pump_host` | 在 pump 之上补一层 task-side remote service facade：继续保留 queue/kick/step/completion 语义，但把任务侧入口收成 `yield_current / sleep_current_until / debug_write / capability_call`，并保留 raw `enqueue(...)` 作为 invalid syscall 与显式 token/sequence 的 escape hatch | 不证明同步阻塞 RPC facade、真正多 outstanding transport、session/channel routing、service discovery、arch ingress、user ABI | 接 `task_message_runtime_api` |
| `TaskMessage runtime API` | `kernel.task_message_runtime_api` | `Examples/kernel/runtime_task_message_runtime_api_host`, `Examples/kernel/runtime_task_message_runtime_service_host` | 在 remote runtime service facade 之上补一层 current-task async runtime naming：把 `yield_current / sleep_current_until` 收成 `yield / sleep_until`，同时保持 `kick / step / receive_completion` 和 pending queue 这些显式 async progress 语义可见 | 不证明最终 async syscall facade、同步阻塞或 future/promise 风格 API、session/channel routing、真正多 outstanding transport、arch ingress、user ABI | 接 `task_message_syscall_api` |
| `TaskMessage syscall API` | `kernel.task_message_syscall_api` | `Examples/kernel/runtime_task_message_syscall_api_host` | 在 async runtime facade 之上再补一层 task-facing async syscall surface：把 `yield / sleep_until / debug_write / capability_call` 收成 `sys_*` 命名，同时保留 `kick / step / receive_completion`、pending queue 与 runtime rebind 可见；需要 raw invalid syscall / explicit token 样本时仍可经由 `runtime().services()` 下钻 | 不证明同步阻塞 RPC facade、future/promise 风格 API、真正多 outstanding transport、arch ingress、user ABI | 接 `task_message_session_api` |
| `TaskMessage session API` | `kernel.task_message_session_api` | `Examples/kernel/runtime_task_message_session_api_host` | 在 async syscall facade 之上第一次长出最小服务对话语义：用 `open / request / close` 管一条 single in-flight session，把 `open reply -> session_handle`、`request reply`、`close timeout -> faulted -> reset()` 收成稳定边界，同时保留向下回到 raw syscall/runtime facade 的逃生口 | 不证明多 session 并发、service discovery、channel routing、阻塞式 RPC/future、真正多 outstanding transport、arch ingress、user ABI | 接 future async RPC / channel facade |
| `TaskMessage session dispatch` | `kernel.task_message_session_dispatch` | `Examples/kernel/runtime_task_message_session_dispatch_host` | 在 server 侧补一条与 session client 对称的最小 ingress seam：把 `capability_call` 形式的 `open / request / close` 路由到 `service_id -> session slot -> handler`，并把 slot 分配、close 回收、unbound handler、unknown handle 和 slot exhaustion 都收成稳定边界；同时已能直接挂进 `task_syscall_table` | 不证明真正 server loop ownership、service discovery、channel registry、多 client 公平性、阻塞式 RPC、arch ingress、user ABI | 接 future server-side session acceptor / channel facade |
| `TaskMessage session acceptor` | `kernel.task_message_session_acceptor` | `Examples/kernel/runtime_task_message_session_acceptor_host` | 在 session dispatch 之上补一层 server-side accepted channel facade：每个 service 一个 acceptor、每个 accepted session 一个 channel handler；把 `dispatcher session_handle -> service-local channel slot`、`open/request/close` 生命周期、slot exhaustion、broken acceptor/unbound channel handler 以及 close 回收都收成稳定边界，并证明它可经由 `task_message_session_service_acceptor_entry(...)` 直接挂回 dispatcher | 不证明 endpoint 语义收口、真正 server loop ownership、多 service/channel registry、公平性策略、阻塞式 RPC、真实 arch ingress、user ABI | 接 `runtime_task_message_session_endpoint_host` |
| `TaskMessage session endpoint` | `kernel.task_message_session_endpoint` | `Examples/kernel/runtime_task_message_session_endpoint_host` | 在 accepted channel facade 之上再补一层 server-side endpoint 语义：把 accepted channel 上下文收成稳定 `endpoint` 视图，把 `accept(endpoint, binding)` 与 `request/close(endpoint view)` 重新桥接回既有 acceptor/channel handler seam，并证明 endpoint 状态可以不泄漏 raw handler 样板地挂回 dispatcher | 不证明真正 server loop ownership、client session facade、多 endpoint/多 client 公平性、阻塞式 RPC、真实 arch ingress、user ABI | 接 `runtime_task_message_session_protocol_host` / `runtime_task_message_session_service_loop_host` |
| `TaskMessage session protocol` | `kernel.task_message_session_protocol` | `Examples/kernel/runtime_task_message_session_protocol_host` | 在 endpoint 语义之上再补一层 server-side operation surface：把 `request.operation` 收成稳定 protocol entry lookup，把 unsupported operation、matched-but-unbound entry、显式 close hook 与默认 close ack 收成可追踪边界，并证明 protocol 可以直接挂回 `endpoint` / `dispatcher` seam | 不证明完整 protocol schema、多 session 并发、多 client 公平性、阻塞式 RPC、真实 arch ingress、user ABI | 接 `runtime_task_message_session_protocol_schema_host` / future typed facade |
| `TaskMessage session protocol schema` | `kernel.task_message_session_protocol_schema` / `kernel.task_message_session_protocol` | `Examples/kernel/runtime_task_message_session_protocol_schema_host` | 在 protocol surface 之上再补一层很薄的 typed/schema facade：把 `operation_name`、`payload field name`、`result_name` 与 `view_kind` 收成稳定 descriptor，并证明 schema handler 可以经由 binding 直接桥回现有 protocol entry | 不证明 payload codec、多参数解码、version negotiation、多 session 并发、阻塞式 RPC、真实 arch ingress、user ABI | 接 `runtime_task_message_session_service_loop_host` / `runtime_task_message_session_roundtrip_host` |
| `TaskMessage session service` | `kernel.task_message_session_service` | `Examples/kernel/runtime_task_message_session_service_host` | 在 server 侧把 `service pump + session dispatcher + session acceptor` 收成一个 ownership facade：统一暴露 `bootstrap/receive/wait/step`、service name、active session/channel inspection 与 trace；证明 service task body 不必直接抓裸 `service_pump` 也能稳定观察 `open/request/close` 进展 | 不证明真正的 service registry、多 service fairness、阻塞式 RPC、真实 arch ingress、user ABI | 接 `runtime_task_message_session_service_loop_host` |
| `TaskMessage session service loop` | `kernel.task_message_session_service` / `kernel.task_message_session_endpoint` / `kernel.task_message_session_protocol_schema` / `kernel.task_message_session_protocol` / `kernel.task_message_syscall_api` | `Examples/kernel/runtime_task_message_session_service_loop_host` | 第一条专门证明 server-side session ownership 已经落到真实 service task loop 的 live seam：client 故意只走 raw `sys_capability_call(...)`，server 则经由 `task_message_session_service -> session_dispatch -> session_acceptor -> session_endpoint -> session_protocol_schema -> session_protocol` 接住 `open/request/close/unsupported open`，并把 service loop 的 wait/timeout/drain/trace 与最终 runtime 清空状态一起收口；同时证明 server handler 已经能看到稳定的 `operation_name / payload field / result_name` | 不证明 `task_message_session_api` 本身的 client facade、多 session 并发、多 client 公平性、阻塞式 RPC、真实 arch ingress、user ABI | 接 `runtime_task_message_session_roundtrip_host` / future channel ownership |
| `TaskMessage session roundtrip` | `kernel.task_message_session_api` / `kernel.task_message_session_service` / `kernel.task_message_session_acceptor` / `kernel.task_message_session_endpoint` / `kernel.task_message_session_protocol_schema` / `kernel.task_message_session_protocol` / `kernel.task_message_session_dispatch` | `Examples/kernel/runtime_task_message_session_roundtrip_host` | 第一条真正把 message-backed session client 和 server-side accepted channel/endpoint/protocol 焊在一起的 live weld：client `open / request / close` 经过 `runtime service -> syscall pump -> frame caller -> task_syscall_table -> session dispatch -> session service -> session acceptor -> session endpoint -> session_protocol_schema -> session_protocol` 到达 server-side protocol entry，再把 `session_handle`、request reply、close reply 和 unsupported open 错误稳定带回 client completion；同时证明 runtime/mailbox/frame/pump、dispatcher session slot 与 accepted channel slot 最终都回到空闲，并且 typed server handler 语义已经进入 live roundtrip | 不证明多 session 并发、多 client 公平性、阻塞式 RPC facade、更真实的 channel registry/ownership、真实 arch ingress、user ABI | 接 future channel ownership / richer operation families |
| `RuntimeTrapServiceFacade` | `kernel.runtime_trap` / `kernel.runtime_service` | `Examples/kernel/runtime_service_host`, `Examples/kernel/runtime_binding_chain_host`, `Examples/kernel/runtime_minimal_host`, `Examples/kernel/runtime_trap_armv7a_host` | task-side trap/service facade 的 `valid()`、`bind_transport(...)`、`yield/sleep/debug/capability` 命名，以及跨 wrapper 链的 transport 重绑定可见性 | 不证明 frame capture / writeback / arch origin | 接 `task runtime API` |
| `TaskRuntimeApi` | `kernel.task_runtime_api` | `Examples/kernel/runtime_task_api_host`, `Examples/kernel/runtime_binding_chain_host`, `Examples/kernel/runtime_minimal_host`, `Examples/kernel/runtime_trap_armv7a_host` | current-task self-service 命名面，`yield()` / `sleep_until(...)` / `debug_write(...)` / `capability_call(...)`，以及 `bind_services(...)` 对顶层 facade 的传播 | 不证明 syscall 编号、dispatch、frame builder | 接 `task syscall API` |
| `TaskSyscallApi` | `kernel.task_syscall_api` | `Examples/kernel/runtime_task_syscall_host`, `Examples/kernel/runtime_binding_chain_host`, `Examples/kernel/runtime_task_syscall_frame_caller_host`, `Examples/kernel/runtime_minimal_host`, `Examples/kernel/runtime_trap_armv7a_host` | syscall-facing 命名面，`sys_*` 语义和 `TaskRuntimeApi` 的分层边界，以及 `bind_runtime(...)` 后顶层 `sys_*` 的切换 | 不证明 catalog / table / frame ingress | 接 `catalog` / `dispatch` |
| `TaskSyscall catalog` | `kernel.task_syscall_catalog` | `Examples/kernel/runtime_task_syscall_catalog_host` | syscall 编号与服务语义的稳定映射 | 不证明具体 handler table 或 frame writeback | 接 `dispatch` |
| `TaskSyscall dispatch` | `kernel.task_syscall_dispatch` | `Examples/kernel/runtime_task_syscall_dispatch_host` | request 到 transport / handler 的稳定分发 | 不证明静态 table 细节 | 接 `table` |
| `TaskSyscall table` | `kernel.task_syscall_table` | `Examples/kernel/runtime_task_syscall_table_host` | syscall number 到 handler 的静态表映射 | 不证明 numbered frame 解析 | 接 `frame` |
| `TaskSyscall frame bridge` | `kernel.task_syscall_frame` | `Examples/kernel/runtime_task_syscall_frame_host` | frame -> request -> table -> writeback 闭环 | 不证明 arch frame 捕获和真实 ingress | 接 `frame caller` 与 `trap ingress` |
| `TaskSyscall frame caller` | `kernel.task_syscall_frame` | `Examples/kernel/runtime_task_syscall_frame_caller_host` | `sys_* -> frame -> table` 的独立 host 闭环 | 不证明真实 leaf frame capture | 接 `trap ingress` |
| `Trap / syscall bridge` | `kernel.runtime_trap` / `kernel.runtime_service` | `Examples/kernel/runtime_minimal_host`, `Examples/kernel/runtime_trap_armv7a_host` | generic host 与 ARMv7-A host 上的 trap/service 证据链 | 不证明真实 leaf frame 的最终写回布局 | 接 `trap ingress` |
| `Trap ingress adapter` | `kernel.runtime_trap_ingress` | `Examples/kernel/runtime_trap_armv7a_host`, `Examples/kernel/runtime_task_syscall_frame_armv7a_host` | frame capture / writeback 的 ingress 入口边界 | 不证明上半层 syscall 语义本身 | 接 ARMv7-A leaf / QEMU 叶子 |
| `ARMv7-A arch ingress seam` | `Examples/kernel/armv7a/qemu` lower-half ingress | `Examples/kernel/armv7a/qemu/run_qemu_arch_ingress_seam_ci.ps1`, `scripts/minimal_kernel_runtime_armv7a_qemu_smoke.ps1` | 同一次 QEMU lower-half run 里观测 `exception/SVC -> timer/IRQ -> context -> runtime-trap -> runtime-loop`，并覆盖 trap `decode -> dispatch -> writeback` | 不证明完整 OS、用户态 ABI、RK3506 真实板级路由或新的 compare 判决模型 | 接 runtime evidence bundle、`kernel_runtime_session` witness 与后续 RK3506 leaf 迁移 |
| `KernelRuntimeSessionWitnessAggregate` | `minimal_kernel.runtime_session_witness_smoke` | `scripts/ci_minimal_kernel_runtime_session_witness_smoke.ps1` | standing `kernel_runtime_session`、synthetic session drift、witness-exported collapsed session 与根 `summary/report/check` 聚合证据包 | 不证明真实 QEMU 新执行路径，也不替代总 runtime evidence bundle | 接 CI / front page / evidence shelf |

## 这页的边界

- 上半层证据优先由 `Examples/kernel/runtime_*_host` 系列承载
- 下半层 ARMv7-A / leaf 证据只在这里做路由引用
- 新增 seam 时，先补这张矩阵，再补对应 verifier
